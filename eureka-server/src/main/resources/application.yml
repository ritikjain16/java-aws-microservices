server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  instance:
    # hostname: localhost
    hostname: eureka-server
  client:
    register-with-eureka: false
    fetch-registry: false
